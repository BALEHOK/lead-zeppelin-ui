# source: http://localhost:4000/graphql
# timestamp: Fri Aug 28 2020 22:58:00 GMT-0400 (Eastern Daylight Time)

"""Exposes a URL that specifies the behaviour of this scalar."""
directive @specifiedBy(
  """The URL that specifies the behaviour of this scalar."""
  url: String!
) on SCALAR

type AccountType {
  id: String
  name: String
  code: String
  funnels: [FunnelType]
  clients: [ClientType]
}

type ClientType {
  id: String
  accountId: String
  name: String
  email: String
  phone: String
  vk: String
  fb: String
  gc: String
}

type FunnelStepType {
  id: String
  name: String
  code: String
}

type FunnelType {
  id: String
  accountId: String
  name: String
  steps: [FunnelStepType]
}

type Mutation {
  createAccount(code: String, name: String): AccountType
  deleteAccount(id: ID): ID
  updateAccount(id: ID, content: String): AccountType
  createFunnel(accountId: String!, name: String!): FunnelType
  createFunnelStep(funnelId: String!, code: String!, name: String!): FunnelType
}

type Query {
  account(code: String!): AccountType
  accounts: [AccountType]
  funnel(id: String!): FunnelType
}
